{
  "name": "pes-marketing-agent",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test tests/*.test.mjs",
    "security-check": "node scripts/security-check.mjs",
    "check": "node --check server.mjs && node --check src/auth.mjs && node --check src/config.mjs && node --check src/policy.mjs && node --check src/provider.mjs && node --check src/service.mjs && node --check public/app.js && npm test && npm run security-check"
  }
}
